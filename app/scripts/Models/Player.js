define([
    'jquery',
    'underscore',
    'backbone'], function($,_,Backbone){
        var Player = Backbone.RaphaelTransformableModel.extend({
            
        });
        return Player;
    }
);